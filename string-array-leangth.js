const myFriend = ['Jubair', 'Ali Haider', 'Shamim Hosain', 'Tarik Islam', 'Ibrahim', 'Ayen Uddin'];

const friendWordLength = myFriend.map(n => n.length);

console.log(friendWordLength);

const friendNameFirstLeter = myFriend.map(name => name[0]);
console.log(friendNameFirstLeter);