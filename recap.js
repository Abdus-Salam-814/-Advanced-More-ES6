/**
 * 1. var let const
 * 2. default parameter
 * 2. template string
 */


const a = 56;
const numbers = [56, 7, 8];
const person ={
    name: 'sakib khan';
}

const message = `Hi, ${person.name} <h1> This is a Heading number 1 </h1>`

if(true){}